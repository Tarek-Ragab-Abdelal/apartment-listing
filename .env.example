# ========================================
# Nawy Apartment Listing - Environment Configuration Template
# ========================================
# Copy this file to .env and update the values for your environment

# ========================================
# Database Configuration
# ========================================
POSTGRES_DB=apartments
POSTGRES_USER=nawy
POSTGRES_PASSWORD=StrongPass

# Database Connection URL (used by API service)
# Format: postgresql://username:password@host:port/database?schema=public
DATABASE_URL=postgresql://nawy:StrongPass@postgres:5432/apartments?schema=public

# ========================================
# API Service Configuration
# ========================================
NODE_ENV=production
API_PORT=4000
API_HOST=0.0.0.0

# JWT Authentication Secret
# Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=placeholder_jwt_secret

# ========================================
# Web Application Configuration
# ========================================
# Backend API URL for Next.js frontend
# For Docker Compose: use service name 'api'
# For external access: use your domain or localhost
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# ========================================
# Docker Network Configuration
# ========================================
# External port mappings (host machine ports)
# Change these if you have port conflicts (I am using 5433 to avoid conflict with local Postgres)
POSTGRES_EXTERNAL_PORT=5433
API_EXTERNAL_PORT=4000
WEB_EXTERNAL_PORT=3000
PRISMA_STUDIO_PORT=5555
